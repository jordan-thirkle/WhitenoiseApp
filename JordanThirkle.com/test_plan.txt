1. Read the memory and constraints for blog posts.
2. Create the first blog post `src/data/blog/app-store-approval-guide.mdx`.
   - Category: "AI"
   - Frontmatter image: `/og/app-store-approval-guide.png`
   - Link to the ranking guide.
   - Include TOC, TS/React snippets (e.g. Expo config), and an ASCII architectural diagram.
3. Create the second blog post `src/data/blog/app-store-ranking-guide.mdx`.
   - Category: "AI"
   - Frontmatter image: `/og/app-store-ranking-guide.png`
   - Link to the approval guide.
   - Include TOC, TS/React snippets (e.g. code to handle App Store reviews API), and an ASCII architectural diagram.
4. Verify both posts visually using the Playwright script as required by `frontend_verification_instructions` in memory, or use `npm run build` & `npx astro check` to verify content structure.
